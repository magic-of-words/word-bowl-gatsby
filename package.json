{
  "name": "word-bowl",
  "version": "1.0.0",
  "private": true,
  "description": "add a word to the bowl then pick one to play. guess the word you picked to win",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build && gatsby-plugin-s3 deploy"
  },
  "dependencies": {
    "gatsby": "^5.13.5",
    "gatsby-plugin-s3": "^0.4.1",
    "gatsby-plugin-sass": "^6.13.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "sass": "^1.77.8"
  }
}
